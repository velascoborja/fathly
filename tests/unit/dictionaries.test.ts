import { describe, expect, it } from "vitest"

import { dictionaries } from "@/lib/i18n/dictionaries"

describe("dictionaries", () => {
  it("uses expenses nomenclature for budget commitments", () => {
    expect(dictionaries.es.loading.title).toBe("Preparando tu presupuesto")
    expect(dictionaries.es.loading.ariaLabel).toBe("Cargando Fathly")
    expect(dictionaries.es.dashboard.title).toBe("Resumen mensual")
    expect(dictionaries.es.dashboard.subtitle).toBe("Resumen del mes teniendo en cuenta ingresos y gastos.")
    expect(dictionaries.es.nav.checkpoints).toBe("Checkpoints")
    expect(dictionaries.es.checkpoints.createAction).toBe("Crear checkpoint")
    expect(dictionaries.es.checkpoints.deleteAction).toBe("Eliminar")
    expect(dictionaries.es.checkpoints.deleteDescription).toContain("{name}")
    expect(dictionaries.es.checkpoints.deleted).toBe("Checkpoint eliminado.")
    expect(dictionaries.es.checkpoints.comparisonTitle).toContain("presupuesto actual")
    expect(dictionaries.es.dashboard.covered).toBe("del presupuesto usado")
    expect(dictionaries.es.dashboard.deposits).toBe("Ingresos")
    expect(dictionaries.es.dashboard.commitments).toBe("Gastos")
    expect(dictionaries.es.dashboard.breakdown).toBe("Distribución por gasto")
    expect(dictionaries.es.dashboard.monthOutflows).toBe("Gastos")
    expect(dictionaries.es.dashboard.monthlyResult).toBe("Resultado mensual")
    expect(dictionaries.es.dashboard.monthlyResultSurplus).toBe("Restante tras gastos")
    expect(dictionaries.es.dashboard.monthlyResultLowMargin).toBe("Margen mínimo")
    expect(dictionaries.es.dashboard.coverageMarginMarker).toBe("Margen mínimo seleccionado")
    expect(dictionaries.es.dashboard.difference).toBe("Diferencia")
    expect(dictionaries.es.settings.lowMonthlyMarginPercent).toBe("Margen mínimo de resultado mensual")
    expect(dictionaries.es.settings.shareAccessCardTitle).toBe("Compartir acceso")
    expect(dictionaries.es.settings.membersTitle).toBe("Personas con acceso")
    expect(dictionaries.es.settings.deleteAccountAction).toBe("Eliminar cuenta")
    expect(dictionaries.es.settings.deleteAccountDialogDescription).toContain("No se puede deshacer")
    expect(dictionaries.es.setup.title).toBe("Asistente inicial de presupuesto")
    expect(dictionaries.es.setup.subtitle).toBe("Añade hasta 2 ingresos mensuales y 2 gastos mensuales para crear el panel. Después podrás completar el resto desde el dashboard.")
    expect(dictionaries.es.setup.stepExpensesBody).toBe("Registra al menos un gasto mensual para activar el panel.")
    expect(dictionaries.es.setup.incomeNameLabel).toBe("Fuente de ingreso")
    expect(dictionaries.es.setup.expenseAmountLabel).toBe("Gasto mensual")
    expect(dictionaries.es.setup.finish).toBe("Crear mi panel")
    expect(dictionaries.es.setup.limitHint).toContain("Máximo 2")
    expect(dictionaries.es.setup.requiredHint).toContain("1 ingreso")
    expect(dictionaries.es.setup).not.toHaveProperty("annualCostsTitle")
    expect(dictionaries.es.dashboard.categoryTotal).toBe("Total")
    expect(dictionaries.es.dashboard.largest).toBe("Mayores gastos")
    expect(dictionaries.es.dashboard).not.toHaveProperty("commandCenter")
    expect(dictionaries.es.actions.editDeposit).toBe("Editar ingreso")
    expect(dictionaries.es.actions.editCommitment).toBe("Editar gasto")
    expect(dictionaries.es.actions.editCategory).toBe("Editar categoría")
    expect(dictionaries.es.actions.renameCategory).toBe("Renombrar categoría")
    expect(dictionaries.es.actions.categoryRenamed).toBe("Categoría actualizada.")
    expect(dictionaries.es.actions.depositCreated).toBe("Ingreso añadido.")
    expect(dictionaries.es.actions.depositSaved).toBe("Ingreso actualizado.")
    expect(dictionaries.es.actions.depositDeleted).toBe("Ingreso eliminado.")
    expect(dictionaries.es.actions.commitmentCreated).toBe("Gasto añadido.")
    expect(dictionaries.es.actions.commitmentSaved).toBe("Gasto actualizado.")
    expect(dictionaries.es.actions.commitmentDeleted).toBe("Gasto eliminado.")
    expect(dictionaries.es.actions.collapseCategory).toBe("Contraer")
    expect(dictionaries.es.actions.expandCategory).toBe("Expandir")
    expect(dictionaries.es.actions.deleteConfirmation).toContain("{name}")
    expect(dictionaries.es.validation.categoryDuplicate).toBe("Ya existe una categoría con ese nombre.")
    expect(dictionaries.es.validation.categoryUnchanged).toBe("El nombre de la categoría no ha cambiado.")
    expect(dictionaries.es.validation.genericCheckpointInvalid).toBe("El checkpoint no es válido.")
    expect(dictionaries.es.monthlyBills.subtitle).toBe("Añade y revisa los gastos mensuales compartidos de la casa.")
    expect(dictionaries.es.annualCosts.subtitle).toBe("Registra los gastos anuales para prorratearlos mes a mes en el presupuesto familiar.")
    expect(dictionaries.es.forms.annualProratedIndicator).toBe("Gasto anual prorrateado")
    expect(dictionaries.es.forms.itemized).toBe("Suma")
    expect(dictionaries.es.forms.itemizedAmountIndicator).toContain("suma de partidas")

    expect(dictionaries.en.loading.title).toBe("Preparing your budget")
    expect(dictionaries.en.loading.ariaLabel).toBe("Loading Fathly")
    expect(dictionaries.en.dashboard.title).toBe("Monthly summary")
    expect(dictionaries.en.dashboard.subtitle).toBe("Review the month across income and expenses.")
    expect(dictionaries.en.nav.checkpoints).toBe("Checkpoints")
    expect(dictionaries.en.checkpoints.createAction).toBe("Create checkpoint")
    expect(dictionaries.en.checkpoints.deleteAction).toBe("Delete")
    expect(dictionaries.en.checkpoints.deleteDescription).toContain("{name}")
    expect(dictionaries.en.checkpoints.deleted).toBe("Checkpoint deleted.")
    expect(dictionaries.en.checkpoints.comparisonTitle).toContain("current budget")
    expect(dictionaries.en.dashboard.covered).toBe("of budget used")
    expect(dictionaries.en.dashboard.commitments).toBe("Shared expenses")
    expect(dictionaries.en.dashboard.breakdown).toBe("Breakdown by expense")
    expect(dictionaries.en.dashboard.monthOutflows).toBe("Monthly outflows")
    expect(dictionaries.en.dashboard.monthlyResult).toBe("Monthly result")
    expect(dictionaries.en.dashboard.monthlyResultSurplus).toBe("Left after expenses")
    expect(dictionaries.en.dashboard.monthlyResultLowMargin).toBe("Minimum margin")
    expect(dictionaries.en.dashboard.coverageMarginMarker).toBe("Selected minimum margin")
    expect(dictionaries.en.dashboard.difference).toBe("Difference")
    expect(dictionaries.en.settings.lowMonthlyMarginPercent).toBe("Minimum monthly result margin")
    expect(dictionaries.en.settings.shareAccessCardTitle).toBe("Share access")
    expect(dictionaries.en.settings.membersTitle).toBe("People with access")
    expect(dictionaries.en.settings.deleteAccountAction).toBe("Delete account")
    expect(dictionaries.en.settings.deleteAccountDialogDescription).toContain("cannot be undone")
    expect(dictionaries.en.setup.title).toBe("Initial budget assistant")
    expect(dictionaries.en.setup.subtitle).toBe("Add up to 2 monthly income items and 2 monthly expenses to create the dashboard. You can finish the rest from the dashboard later.")
    expect(dictionaries.en.setup.stepExpensesBody).toBe("Register at least one monthly expense to enable the dashboard.")
    expect(dictionaries.en.setup.incomeNameLabel).toBe("Income source")
    expect(dictionaries.en.setup.expenseAmountLabel).toBe("Monthly expense")
    expect(dictionaries.en.setup.finish).toBe("Create my dashboard")
    expect(dictionaries.en.setup.limitHint).toContain("Maximum 2")
    expect(dictionaries.en.setup.requiredHint).toContain("1 income")
    expect(dictionaries.en.setup).not.toHaveProperty("annualCostsTitle")
    expect(dictionaries.en.dashboard.categoryTotal).toBe("Total")
    expect(dictionaries.en.dashboard.largest).toBe("Largest expenses")
    expect(dictionaries.en.dashboard).not.toHaveProperty("commandCenter")
    expect(dictionaries.en.actions.editDeposit).toBe("Edit income")
    expect(dictionaries.en.actions.editCommitment).toBe("Edit expense")
    expect(dictionaries.en.actions.editCategory).toBe("Edit category")
    expect(dictionaries.en.actions.renameCategory).toBe("Rename category")
    expect(dictionaries.en.actions.categoryRenamed).toBe("Category updated.")
    expect(dictionaries.en.actions.depositCreated).toBe("Income added.")
    expect(dictionaries.en.actions.depositSaved).toBe("Income updated.")
    expect(dictionaries.en.actions.depositDeleted).toBe("Income deleted.")
    expect(dictionaries.en.actions.commitmentCreated).toBe("Expense added.")
    expect(dictionaries.en.actions.commitmentSaved).toBe("Expense updated.")
    expect(dictionaries.en.actions.commitmentDeleted).toBe("Expense deleted.")
    expect(dictionaries.en.actions.collapseCategory).toBe("Collapse")
    expect(dictionaries.en.actions.expandCategory).toBe("Expand")
    expect(dictionaries.en.actions.deleteConfirmation).toContain("{name}")
    expect(dictionaries.en.validation.categoryDuplicate).toBe("A category with that name already exists.")
    expect(dictionaries.en.validation.categoryUnchanged).toBe("The category name has not changed.")
    expect(dictionaries.en.validation.genericCheckpointInvalid).toBe("Invalid checkpoint.")
    expect(dictionaries.en.monthlyBills.subtitle).toBe("Add and review the household's shared monthly expenses.")
    expect(dictionaries.en.annualCosts.subtitle).toBe("Track annual costs so they are prorated month by month in the household budget.")
    expect(dictionaries.en.forms.annualProratedIndicator).toBe("Prorated annual expense")
    expect(dictionaries.en.forms.itemized).toBe("Sum")
    expect(dictionaries.en.forms.itemizedAmountIndicator).toContain("sum of items")
  })
})
